/* acidcards.csv: Tier 1 minions and their effect overrides. */
(() => {
  window.AcidCardModules.register({
    kind: 'minion',
    tier: 1,
    label: 'ティア1・ミニオン',
    effects: {
      // 'カード名': card => ({ battlecry(gameState) { ... } }),
    },
  });
})();